const { TeammemberList } = require('./teammember.model');
const { TeamList } = require('./team.model');
const { ProjectList} = require('./project.model');
const { TaskList} = require('./task.model');

module.exports = {
   TeammemberList,
   TeamList,
   ProjectList,
   TaskList
}